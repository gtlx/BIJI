# Biji Note 分期规划(2026-08-17 更新:定位升级——块级存储)

> 状态:M0 文档定稿;M1 web 端基础 ✅;M2 块级存储核心 ✅(2026-08-17);M4.5 工作区 Pane 界面改造 ✅(2026-08-19);前期**以 web 端为主**(Tauri 壳后置)
> 健康检查(2026-08-17):后端 cargo check/test 全绿(现有 + M2 新增);前端 tsc 0;M1 已知坑见 biji-project-dev 技能

## 定位(2026-08-17 用户拍板:合并长处,非 Obsidian 仿品)

**Obsidian 文件布局(改进)+ Logseq 块级思想 + 块时间戳演变 = BIJI 灵魂**

- 为什么:Obsidian 检索复杂、文件多了乱、**没有块级时间戳**;Logseq 布局乱——BIJI 取两者长处,独有卖点 = **每段话(块)带时间戳,可看笔记演变**
- **存储:默认数据库(SQLite 块级)**——每个块 = {内容, created_at, updated_at, 历史[]};数据库是真相源
- **导出:才生成文件夹**——按 Obsidian 式文件布局导出一笔记一 .md(块时间戳以标记/注释嵌入,Obsidian 兼容可打开)
- **文件夹:嵌套可折叠树**(不学 Obsidian 全展开)——文件夹多时折叠收纳,展开才看子级
- 检索:简化 Obsidian 的复杂检索(FTS5 已内置,界面交互后续简化讨论)

## 数据模型(块级升级方向)

```
笔记(note) = 元数据(frontmatter)+ 块序列
块(block) = {id, note_id, parent_id(嵌套块), type(段落/标题/列表), content, created_at, updated_at, order}
块历史(block_history) = {block_id, content_snapshot, changed_at, change_type}
渲染:块序列 → markdown 展示(段落/标题分块)
编辑:块粒度 diff——改哪段只盖哪段时间戳
演变视图:块旁时间戳 + 点块看历史 / 笔记按块时间线重排
导出:块时间戳以 HTML 注释/特殊标记写入 .md(Obsidian 打开正常,时间戳可见)
```

## 分期

| 阶段 | 内容 | 状态 |
|---|---|---|
| **M1 web 端基础** | tsc 修复 + 商枢风格布局重构 + 自适应 | ✅ 完成 |
| **M2 块级存储**(新核心) | 块表 + 历史表 + 迁移拆块 + 块服务 + 双模式搜索 | ✅ 完成 |
| **M3 演变与导航** | 演变视图(块历史/时间线重排)+ 嵌套文件夹树 + 搜索体验简化 | ✅ 完成(2026-08-17):块历史弹层(时间戳+快照+变更类型)、演变模式时间线重排+高亮、块时间戳可点+悬停;嵌套可折叠文件夹树(默认收两层+树顶过滤+localStorage 展开记忆)+编辑器顶部面包屑(可点跳层级);搜索双模式异步(title/内容按块命中+片段高亮+块时间戳+点击跳笔记) |
| **M3.5 体验增强包**(2026-08-17 用户批准) | ①**日历 + 块热力图** ②**反向链接面板** ③**标签树/过滤** ④**回收站/软删除** ⑤**笔记模板** ⑥**PDF/导出增强** ⑦**暗色主题/排版** | ①②③ ✅ 完成(2026-08-17):日历月历热力图(块数越多色越深 薄荷绿→teal,点天看当天块+跳笔记)、反向链接面板(块级:来源笔记+片段+块时间戳,点跳来源)、标签树(侧栏标签区+展开下笔记+点击过滤 NoteList)。④⑤⑥⑦ ✅ 完成(2026-08-18,commit 见下):④回收站(笔记/块软删 deleted_at+迁移 v3,列/恢复回原笔记/彻底删/清空,侧栏回收站视图,恢复/彻底删除/清空+二次确认)⑤笔记模板(内置 空白/日记/会议/读书 + 用户自定义,存 templates 表,新建笔记选模板弹窗,{{date}} 自动替换)⑥导出增强(单笔记导出干净 .md;导出可打印 HTML——PDF 走浏览器「打印为 PDF」,后端不引入重渲染引擎)⑦暗色主题/排版(CSS 变量已 tokens 化+深色薄荷绿 teal 配色,自动跟随系统 + 设置手选浅色/深色/跟随系统;字号/字体设置全局生效) |
| **M4 导出与同步** | **发布**(静态站点 Hugo/Astro/VitePress)+ **Git**(管导出文件夹:版本快照/发布/双链同步,块历史在 DB)+ 云同步(webdav/库文件)+ 插件系统完善 | **首批(发布+Git)✅(2026-08-18)**:Git 导出版本(库→Obsidian md 文件夹→git add/commit/log,块时间戳 HTML 注释)+ 静态站点发布向导(选生成器/检查可用性/确认发布)+ 后端 Git/Publish 服务补全单测;Web/Mock 展示流程,真实 git/生成器执行留 M6 接壳 |
| **M4.5 工作区界面改造**(2026-08-18 用户拍板,2026-08-19 完成) | 工作区 **Obsidian 式 Pane 面板化** + 模板完善 + 全局快捷键 | ✅ **完成(2026-08-19)**:工作区改多栏 Pane 布局——每模块面板一块(编辑器/文件/大纲/反向链接/图谱/日历),横向多栏并存、栏宽拖拽(drag divider)、面板标题栏拖动重排(同栏换位/跨栏移动/拖到边缘新栏)、布局 localStorage 记忆(刷新/重启还原)、可开关/添加面板;模板一键插入当前笔记光标处({{date}} 变量替换);新建模板选模板弹窗支持内容预览+自定义模板删除;全局快捷键(Ctrl/Cmd+K 或 / 搜索、Ctrl/Cmd+S 保存、Ctrl/Cmd+P 命令面板列出新建/搜索/切面板/导出/插模板等;新建/日记/大纲/图谱保留);移动端不受影响(底栏单栏,图谱/日历仍全屏可开) |
| **M5 AI** | 块级检索上下文 → LLM(AI 问答带出处/摘要/语义检索),`AiProvider` trait 落地 | 待做 |
| **M6 Tauri 壳** | 复用同一前端打包桌面壳,接真实 Git/发布(现有 Tauri 命令) | 待做 |
| **M7 写作/维护双模式**(2026-08-19 自述前瞻) | **写作模式**(极简专注界面,只管写)+ **维护模式**(现有界面:文件/标签/索引/AI 整理)。**同一份数据,两种模式一键切换**(非两套系统):写作时隐藏维护元素,维护时全功能整理。AI 辅助索引/标签,避免"维护地狱" | 待做(自述见 README「项目自述」) |
| **M7.5 文档模式**(2026-08-24 确认雏形已存在) | **块级 + 长文混合视图**:Editor.tsx 的 `useBlockRender = showBlockTimestamps || timelineMode`——关闭块时间戳 + 非演变模式时走**整篇连续 markdown 渲染**(块边界隐藏,数据不变),即文档模式雏形已实现。**产品化待做**:①显式「文档/块」视图切换(现在只是时间戳开关的副作用,应成独立概念)②文档模式阅读排版(阅读宽度/标题锚点/TOC)③与 M7 写作模式联动(写作模式默认文档视图) | 雏形已有,产品化待做 |
| **M8 前端插件化** ✅(2026-08-24) | 后端已插件化(发布=publish-plugin ✅),**前端补全注册表**:新建 `frontend/src/plugins/registry.tsx`(克制版接口:id/label/icon/kind/renderView/paneId),`PublishPanel` 注册为内置 view 型插件,**App.tsx 去掉发布三处硬编码**(导航项/渲染分支/组件挂载三处 → 注册表生成 + 查表渲染),导航从 CORE + 插件 + 工具三项拼接;`看板(kanban)` 注册为 pane 型插件样板,经 Pane 系统渲染。PluginManagerModal 纳入前端插件留后续(克制,未做) | 发布=端到端插件样板(publish-plugin 后端能力 + 前端注册表);看板=前后端双样板(KanbanPane + frontmatter status) |
| **M8 补：插件管理整合前端插件** ✅(2026-08-24) | 当前 PluginManagerModal(插件管理弹窗)**只读后端插件**(`backend.getPlugins()` → biji-core built_in_plugins:pomodoro/sync/publish),**前端插件(发布/看板,registry.tsx)没接进去**,所以插件管理看不到它们。**要做**:把 PluginManagerModal 扩展成「后端插件 + 前端插件」合并显示——前端插件(registry.tsx)也列出并带开关(enable 状态伪持久或接前端 state),让发布/看板在插件管理里可见可管理 | 已完成:PluginManagerModal 合并后端+前端插件显示(来源徽标后端/前端);前端插件开关经 registry localStorage+订阅,关闭→导航/渲染实时消失(CDP 实测通过) |
| **M8 备注：动态加载插件包 —— 明确不做(用户决定)** | Rust 的 `.so` 二进制动态加载跨 Tauri/dylib 边界**很重且易碎**,克制版**不做真二进制加载**。后端 `PluginManager._plugins_dir` 字段已预留但未实现扫描。**将来若有真实第三方插件需求**,再走「声明驱动」(扫描 plugins_dir 读插件清单 JSON/TOML 并入管理),声明驱动比二进制加载轻得多 | 明确不做/待真实需求 |
| **M8 收尾完成(2026-08-25,A+B+C 全落地)** | **A 声明驱动插件加载**(838e4a1):后端扫描 `plugins/` 下 plugin.json(子目录或同级 .json)并入内置插件列表统一 toggle;plugin_state 表持久化 enabled(幂等建表,不破坏 user_version 门控);目录空/不存在静默。**B 看板补全**(c1a98cf):新建卡片(生成 frontmatter status 空笔记)、改标题(行内编辑)、列内上下排序(localStorage 记忆,按钮式零拖拽库)。**C 添加面板接 enable**(c1a98cf):PANE_META 插件面板仅当对应插件启用可添加,禁用则从「添加面板」菜单消失。CDP 实测:新建/改名/排序/禁用插件→菜单消失 全通过 | A+B+C 全部完成并 push |
| **M8 演进：非克制版的插件解耦(借鉴 deepseek-harness/Cordis,后续规划,近期不做)** | 用户认可「不要一直克制版」,但**当前余额有限、以收尾为主,此演进记规划不实现**。借鉴点(轻量,不引入 Cordis):①**前端轻量事件总线做插件间解耦**——现在插件间直接 import(如 kanban 调 backend.saveNote),改为「插件 emit 事件 → 其他插件监听响应」(对应 dsh 的类型化事件 bus,避免硬调依赖)②**插件注册可逆(disposer)**——每个前端插件注册带 cleanup,面板关闭/禁用时自动解除注册(对应 Cordis 的 dispose/热卸载语义)。**不引入 Cordis 框架、不做运行时插件树/依赖注入/HMR**——那对笔记编辑器是过度设计,且等于用户刚拒绝的动态加载。定位:dsh 是 Agent 运行时的「万物皆插件生态」,biji 是「能力解耦」,量级不同,只借解耦思想 | 待做(收尾期结束、有真实多插件需求再启动) |
| **M9 主题系统**(2026-08-22 用户确认) | **可更换主题**:现有 CSS 变量已 tokens 化(浅色/深色/跟随系统),扩展为多主题——内置主题(薄荷绿/暗色等)+ 用户自定义主题(配色变量),设置页选择,全局生效 | 待做 |
| **M10 元数据 + 模板导入 + 右 dock 模块纯净**(2026-08-22 用户确认) | ①**Obsidian 式元数据**:笔记 frontmatter(标题/标签/别名/日期等)编辑与展示,发布时映射 ②**导入笔记模板**:从 .md 文件导入自定义模板 ③**右 dock 模块纯净(用户重点)**:RightPanel 目前是"大纲/反向链接/属性/番茄钟"共用的一个组件(内置 tab 栏,App.tsx renderPane 传 defaultTab),导致拖出某模块时其内部还显示其他模块的信息——**需拆开:每个模块独立 PaneId + 独立渲染,只显示本模块信息,不混其他模块内容** | ③**右 dock 模块独立 ✅(2026-08-25,b9d4ae9)**:新建 OutlinePane/BacklinksPane/PropertiesPane 各独立单内容组件,删 RightPanel.tsx(多 tab 已废弃);属性面板独立 PaneId('properties')+ 真实 frontmatter 编辑(标题/状态/标签,writeFrontmatter 写入器,与看板 status 解析互通);defaultLayout 把 properties 入 hidden 可经添加面板开。tsc 0 + vite build 0。①② 待做 |
| **M11 看板模式**(2026-08-24 用户确认,查看分析类) | **笔记库仪表盘/整体预览**:①库统计(笔记数/块数/总字数/标签数,SQL 聚合)②时间线演变(块创建/修改时间分布,复用日历热力图数据思路)③关系图谱(复用 GraphView)④最近活动(最近编辑/新块/新笔记)⑤写作连击(连续写作天数,可选)。**插件化**:后端 `provides:["analytics"]` 能力插件 + 前端面板插件——与发布插件同构,作为 **M8 前端插件化的第一个真实样板**(发布=后端样板,看板=前后端双样板)。**看板插件基线已做(2026-08-24)**:KanbanPane(待办/进行中/已完成三列,卡片=笔记标题,点开=打开笔记,左右按钮在列间流转;状态存笔记 content 的 frontmatter `status:`,后端 NoteFrontmatter 加 status 字段+parse+导出写,不进块);注册为 pane 型前端插件+Pane 面板 | 插件基线 ✅;①②③④⑤仪表盘子项待做 |

> **M4.5 补(2026-08-19 二次迭代,用户拍板「只需模块化、分栏,核心逻辑不动」)**:Pane 升级为 **固定主区 + 可切分右 dock**(保持业务组件/文件/标签逻辑不动,只改布局容器):
> - **布局模型 v2(main/left/right)**:左 dock(文件树)+ 主区(编辑器)**固定不参与分栏**(用户拍板:这俩位置/逻辑已好);右 dock(大纲/反向链接/标签/图谱/日历/番茄钟)**可上下分栏 + tab + 拖拽**。`PANE_META` 加 `zone` 字段分区;layoutStore v1(columns)→v2 自动迁移
> - **右 dock 能力(CDP 实测通过)**:①多 row = 上下分栏(拖 tab 到 dock 顶部/底部新建行)②同 row 多面板 = tab 标签页(点切换)③拖拽:tab 跨 row 合并/换位/上下分块,`pane-hdivider` 拖调上下高度;「+」可恢复隐藏面板
> - **主区/左 dock 锁定**:App 的 ensure/close/toggle 按 `zone` 分流——editor/files 固定不可关,右 dock 面板可开关
> - **发布改造(2026-08-19)**:`PublishConfig` 新增 `target_dir` 主路径——**发布到用户现有博客目录,不绑定任何框架**(导出 md→该目录,构建交你博客自己);旧的「自建站点+生成器(hugo/astro/vitepress)」降级为可选高级项。前端 PublishPanel 改「发布到已有博客目录(主)/自建站点(高级)」双模切换。单测 6 通过(cargo test 全绿,含 target_dir 两个新用例),前端 tsc 0

> 原「双向链接/图谱/标签」并入 M2/M3(块级后 wikilink 作用于块,图谱按块关系)

## Git/发布/同步现状(2026-08-17 核查;M4 首批已实现 2026-08-18)

- 后端 `services/git.rs`(libgit2:init/status/commit/log)+ 新增 `export_and_commit`(库→Obsidian md 导出文件夹→git add/commit)已补全并单测(真实 libgit2 临时仓库走通)
- 后端 `services/publish.rs`(静态站点 Hugo/Astro/VitePress):`check_generator`/`publish` 补全,生成器缺失时降级返回(不崩溃、提示未安装);`write_notes_to` 纯文件逻辑可单测
- 前端 web 模式(MockBackend)的 git/publish 方法:git 状态/导出并提交/发布向导为内存假数据(标注 Mock);Tauri 命令 `git_export_commit` 已注册(占位,M6 接壳)
- 云同步(webdav/库文件)+ 插件系统完善:M4 后续批次,排期

## 决策(2026-08-17 全部定稿,用户拍板)

- 前期只做 web 端;UI 风格对齐商枢/bill(薄荷绿毛玻璃、内联 SVG、自适应三档)
- 布局形态:桌面左侧栏 + 移动端底部 Tab 栏(双形态,断点 lg)
- **存储真相源 = SQLite;文件夹只是导出形态**(双向:库↔文件夹互转,兼容 Obsidian 等纯文本笔记)
- **嵌套文件夹:混合模式(定稿,不做设置项)**——左侧可折叠树(默认收到两层,树顶过滤框按标题过滤)+ 编辑器顶部面包屑(当前路径可点跳);展开/收起状态记忆
- **文档模式(2026-08-24 确认)**:编辑器双视图——块视图(时间戳/演变/块历史)与文档视图(连续长文,块数据不变仅隐藏)。**同一份数据两种形态**,这是「块级+长文混合」的差异化卖点:Obsidian 无块时间戳、Logseq 无长文阅读体验,BIJI 两者兼得
- **导出时间戳格式:HTML 注释**(`<!-- biji:block 2026-08-17T02:30:00 -->`,阅读模式干净,Obsidian/Typora 兼容;导入时读注释恢复时间戳,无注释按 mtime 赋初值)
- **检索双模式**:标题检索(树过滤,找文件)/ 全文检索(找内容,FTS5 两块索引);**全文检索按块返回**(命中块 + 所在笔记 + 片段高亮)
- **AI 接口预留**:内核 `AiProvider` trait 抽象(检索上下文→LLM),第一期只留接口,后续语义检索/AI 问答/摘要
- **API 接口(bill 同款)**:内核能力全部暴露 REST API(笔记/块/历史/搜索/导出),前端走 API(本地服务模式),外部可定制
- 双向链接/图谱/标签并入 M2/M3(块级后 wikilink 作用于块,图谱按块关系)
