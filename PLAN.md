# Biji Note 分期规划(2026-08-17 更新:定位升级——块级存储)

> 状态:M0 文档定稿;M1 web 端基础 ✅;M2 块级存储核心 ✅(2026-08-17);前期**以 web 端为主**(Tauri 壳后置)
> 健康检查(2026-08-17):后端 cargo check/test 全绿(现有 + M2 新增);前端 tsc 0;M1 已知坑见 biji-project-dev 技能

## 定位(2026-08-17 用户拍板:合并长处,非 Obsidian 仿品)

**Obsidian 文件布局(改进)+ Logseq 块级思想 + 块时间戳演变 = BIJI 灵魂**

- 为什么:Obsidian 检索复杂、文件多了乱、**没有块级时间戳**;Logseq 布局乱——BIJI 取两者长处,独有卖点 = **每段话(块)带时间戳,可看笔记演变**
- **存储:默认数据库(SQLite 块级)**——每个块 = {内容, created_at, updated_at, 历史[]};数据库是真相源
- **导出:才生成文件夹**——按 Obsidian 式文件布局导出一笔记一 .md(块时间戳以标记/注释嵌入,Obsidian 兼容可打开)
- **文件夹:嵌套可折叠树**(不学 Obsidian 全展开)——文件夹多时折叠收纳,展开才看子级
- 检索:简化 Obsidian 的复杂检索(FTS5 已内置,界面交互后续简化讨论)

## 数据模型(块级升级方向)

```
笔记(note) = 元数据(frontmatter)+ 块序列
块(block) = {id, note_id, parent_id(嵌套块), type(段落/标题/列表), content, created_at, updated_at, order}
块历史(block_history) = {block_id, content_snapshot, changed_at, change_type}
渲染:块序列 → markdown 展示(段落/标题分块)
编辑:块粒度 diff——改哪段只盖哪段时间戳
演变视图:块旁时间戳 + 点块看历史 / 笔记按块时间线重排
导出:块时间戳以 HTML 注释/特殊标记写入 .md(Obsidian 打开正常,时间戳可见)
```

## 分期

| 阶段 | 内容 | 状态 |
|---|---|---|
| **M1 web 端基础** | tsc 修复 + 商枢风格布局重构 + 自适应 | ✅ 完成 |
| **M2 块级存储**(新核心) | 块表 + 历史表 + 迁移拆块 + 块服务 + 双模式搜索 | ✅ 完成 |
| **M3 演变与导航** | 演变视图(块历史/时间线重排)+ 嵌套文件夹树 + 搜索体验简化 | ✅ 完成(2026-08-17):块历史弹层(时间戳+快照+变更类型)、演变模式时间线重排+高亮、块时间戳可点+悬停;嵌套可折叠文件夹树(默认收两层+树顶过滤+localStorage 展开记忆)+编辑器顶部面包屑(可点跳层级);搜索双模式异步(title/内容按块命中+片段高亮+块时间戳+点击跳笔记) |
| **M3.5 体验增强包**(2026-08-17 用户批准) | ①**日历 + 块热力图** ②**反向链接面板** ③**标签树/过滤** ④**回收站/软删除** ⑤**笔记模板** ⑥**PDF/导出增强** ⑦**暗色主题/排版** | ①②③ ✅ 完成(2026-08-17):日历月历热力图(块数越多色越深 薄荷绿→teal,点天看当天块+跳笔记)、反向链接面板(块级:来源笔记+片段+块时间戳,点跳来源)、标签树(侧栏标签区+展开下笔记+点击过滤 NoteList)。④⑤⑥⑦ ✅ 完成(2026-08-18,commit 见下):④回收站(笔记/块软删 deleted_at+迁移 v3,列/恢复回原笔记/彻底删/清空,侧栏回收站视图,恢复/彻底删除/清空+二次确认)⑤笔记模板(内置 空白/日记/会议/读书 + 用户自定义,存 templates 表,新建笔记选模板弹窗,{{date}} 自动替换)⑥导出增强(单笔记导出干净 .md;导出可打印 HTML——PDF 走浏览器「打印为 PDF」,后端不引入重渲染引擎)⑦暗色主题/排版(CSS 变量已 tokens 化+深色薄荷绿 teal 配色,自动跟随系统 + 设置手选浅色/深色/跟随系统;字号/字体设置全局生效) |
| **M4 导出与同步** | **发布**(静态站点 Hugo/Astro/VitePress)+ **Git**(管导出文件夹:版本快照/发布/双链同步,块历史在 DB)+ 云同步(webdav/库文件)+ 插件系统完善 | **首批(发布+Git)✅(2026-08-18)**:Git 导出版本(库→Obsidian md 文件夹→git add/commit/log,块时间戳 HTML 注释)+ 静态站点发布向导(选生成器/检查可用性/确认发布)+ 后端 Git/Publish 服务补全单测;Web/Mock 展示流程,真实 git/生成器执行留 M6 接壳 |
| **M5 AI** | 块级检索上下文 → LLM(AI 问答带出处/摘要/语义检索),`AiProvider` trait 落地 | 待做 |
| **M6 Tauri 壳** | 复用同一前端打包桌面壳,接真实 Git/发布(现有 Tauri 命令) | 待做 |

> 原「双向链接/图谱/标签」并入 M2/M3(块级后 wikilink 作用于块,图谱按块关系)

## Git/发布/同步现状(2026-08-17 核查;M4 首批已实现 2026-08-18)

- 后端 `services/git.rs`(libgit2:init/status/commit/log)+ 新增 `export_and_commit`(库→Obsidian md 导出文件夹→git add/commit)已补全并单测(真实 libgit2 临时仓库走通)
- 后端 `services/publish.rs`(静态站点 Hugo/Astro/VitePress):`check_generator`/`publish` 补全,生成器缺失时降级返回(不崩溃、提示未安装);`write_notes_to` 纯文件逻辑可单测
- 前端 web 模式(MockBackend)的 git/publish 方法:git 状态/导出并提交/发布向导为内存假数据(标注 Mock);Tauri 命令 `git_export_commit` 已注册(占位,M6 接壳)
- 云同步(webdav/库文件)+ 插件系统完善:M4 后续批次,排期

## 决策(2026-08-17 全部定稿,用户拍板)

- 前期只做 web 端;UI 风格对齐商枢/bill(薄荷绿毛玻璃、内联 SVG、自适应三档)
- 布局形态:桌面左侧栏 + 移动端底部 Tab 栏(双形态,断点 lg)
- **存储真相源 = SQLite;文件夹只是导出形态**(双向:库↔文件夹互转,兼容 Obsidian 等纯文本笔记)
- **嵌套文件夹:混合模式(定稿,不做设置项)**——左侧可折叠树(默认收到两层,树顶过滤框按标题过滤)+ 编辑器顶部面包屑(当前路径可点跳);展开/收起状态记忆
- **导出时间戳格式:HTML 注释**(`<!-- biji:block 2026-08-17T02:30:00 -->`,阅读模式干净,Obsidian/Typora 兼容;导入时读注释恢复时间戳,无注释按 mtime 赋初值)
- **检索双模式**:标题检索(树过滤,找文件)/ 全文检索(找内容,FTS5 两块索引);**全文检索按块返回**(命中块 + 所在笔记 + 片段高亮)
- **AI 接口预留**:内核 `AiProvider` trait 抽象(检索上下文→LLM),第一期只留接口,后续语义检索/AI 问答/摘要
- **API 接口(bill 同款)**:内核能力全部暴露 REST API(笔记/块/历史/搜索/导出),前端走 API(本地服务模式),外部可定制
- 双向链接/图谱/标签并入 M2/M3(块级后 wikilink 作用于块,图谱按块关系)
